<event_chain>
 -if({{user}}明确接受"讨伐赫卡忒"任务){进行下一步}else{<!-- 如果用户选择其他任务或拒绝赫卡忒任务 -->事件链结束, 你需要在本轮的<UpdateVariable>中_.set('事件链.结束[0]','true');}
 -<% if (getvar('stat_data.角色.状态.等级[0]') >= 8) { %>你需要指出赫卡忒的传说早已随着时间流逝而变得模糊，甚至被认为是虚构的。如今的血族势力衰微，这次的“讨伐”更像是一次对历史遗迹的清扫。事件链因此结束, 你需要在本轮的<UpdateVariable>中_.set('事件链.结束[0]','true');<% } else { %>{{user}}对即将到来的命运一无所知，故事正式拉开序幕。你需要在本轮的<UpdateVariable>中_.set('事件链.大狗叫[0]','道中');<% } %>
 -在踏上旅途前向{{user}}询问是否真的要继续这个危险的任务？现在回头还来得及。if({{user}}选择不继续任务){事件链结束,你需要在本轮的<UpdateVariable>中_.set('事件链.结束[0]','true');}
</event_chain>
